"""A7 适配器层（P8 业务专属）。

存放 P8 业务与外部通道（飞书等）之间的**业务专属**适配层。

与 ``feishu_gateway_cli/``（通用飞书通道封装）的边界：
    - ``feishu_gateway_cli/`` 是通道级工具（不关心业务；可被 P1-P10 共用）
    - ``A7/adapters/`` 是业务级适配（强耦合 P8；只服务 P8 业务）

本包按需新增模块，目前包含：
    - ``chat_reply``：飞书侧 → P8 Agent → 飞书侧的回复适配器（§7.5 时序）
"""