{
  "name": "openclaw-channel-gateway-standalone",
  "version": "0.1.0",
  "private": true,
  "description": "A standalone channel gateway boundary extracted from OpenClaw architectural concepts, with REST, SSE, generic webhook and Feishu/Lark adapters.",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "start": "node src/index.js",
    "test": "node --test --test-concurrency=1",
    "check": "bash scripts/check.sh",
    "smoke": "bash scripts/smoke.sh"
  },
  "license": "MIT"
}
