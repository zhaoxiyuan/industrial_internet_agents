# 环境变量（包含敏感信息）
.env
.env.local
.env.*.local

#日志
A5\logs_test
A5\logs

# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
dist/
build/

# IDE
.idea/
.vscode/
*.swp
data/jobs/
data/output
data/input
.mcp.json
agents/data

.env.bak
model_profiles.json
frontend/saved_configs.json
openclaw-channel-gateway-standalone/data/gateway-state.json
openclaw-channel-gateway-standalone/data/gateway-state.json
openclaw-channel-gateway-standalone/data/gateway-state.json
openclaw-channel-gateway-standalone/.gateway.pid
openclaw-channel-gateway-standalone/data/gateway-state.json

# 2026-08-17：运行时数据 / 备份 / 误存文件不入仓

# 卡片回调审计日志（每次点击追加）
/data/card_callbacks.jsonl

# 飞书卡片运行时索引（本机自动生成，不跨环境同步）
/data/feishu_card_index.json

# 本地 GitHub API 辅助脚本（读取本机 .env 凭据）
/_github_helper.py

# Claude/Codex 本机权限与偏好设置
/.claude/settings.local.json

# 各处备份文件
*.bak
**/*.bak

# pip install 误存到当前目录的 `=1.x.y` 空文件 / 日志
/=1.*.*
logs/web_server_8080.log

# P8 运行时日志（daemon / 测试运行产生，体积大且不可复现）
data/runtime/
A5/log_test/
A6/log_test/
A5/logs/
A6/logs/

# A7 运行时日志（chat_reply daemon / web server）
A7/data/

# 外部独立仓库（cc-connect：单独 git 仓库，126MB；通过 git clone 单独管理）
"cc connect/"
cc connect/


a/A6_A7/a6_runtime/log_test/
a/A5/log_test/
a/A6_A7/a6_runtime/
A6_A7/a6_runtime/

# === 本机一次性产物(2026-09-20 加)===

# 测试临时目录(体积大,git status 警告 Permission denied)
.test-tmp/

# 备份/工作目录
.backups/
.backup/
.workbuddy/
backups/

# 一次性临时文件(我自己调试用)
_*.txt
_*.log
_*.py
/=*.*.txt

# 进程 stdout/stderr 跳转日志
*.err.log
*.out.log
web/*.log
P8P9/*.log
a/P8P9/*.log
a/web/*.log
a/A7/*.log

# 临时数据 / 一次性 JSON
data_integration/
r3.json

# 手测脚本(不算单元测试)
tests/manual_*.py

# 根目录 a 副本(以 a/aaaseverstart.py 为准)
aaaseverstart.py

# a/ 子项目专用(2026-09-20 加)
# LLM 配置(含 API key,不能入仓)
a/agent_config/saved_configs.json
a/frontend/saved_configs.json
# 运行时数据(对齐根 data/jobs + data/runtime)
a/data/jobs/
a/data/runtime/
